apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: default
spec:
  selector:
    app: postgres       # ← must match the Deployment's pod labels
  ports:
    - port: 5432
      targetPort: 5432